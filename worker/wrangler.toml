name = "yggbagle-worker"
main = "src/index.js"
compatibility_date = "2024-11-01"

# 這兩個是「非機密」設定，可以直接寫在這裡並 commit 進 repo。
# 機密（Service Account 金鑰）一律用 `wrangler secret put` 設定，不要寫在這個檔案。
[vars]
SPREADSHEET_ID = "請填入你的 Google Sheets 試算表 ID"
